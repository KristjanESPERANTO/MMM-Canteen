const config = {
  tabWidth: 2,
  useTabs: false,
  overrides: [
    {
      files: "*.md",
      options: {
        parser: "markdown"
      }
    }
  ],
  trailingComma: "none"
};

export default config;
